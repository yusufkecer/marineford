name: marineford_bench
description: >-
  Reproduces the cost model marineford's design decisions are based on.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  dart_eval: ^0.8.5
  marineford_core: ^0.1.0-dev
  pub_semver: ^2.1.5
